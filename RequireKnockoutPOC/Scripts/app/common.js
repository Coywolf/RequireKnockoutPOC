define(['jquery', 'knockout', 'componentLoader'], function ($, ko) {
    $.noConflict(); // this removes window.$, which means all modules would have to require jquery like this one does

    // special component registration should go here
});